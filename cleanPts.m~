function [points]=cleanPts(pts)
    len = size(pts,2);
    counter =1;
    a = [];
for i =1:len
    
        if(pts(1,i)==pts(2,i)&&pts(1,i)==0)
           a(counter) = i;
        end
        counter = co
end
for i = 1:size(a,2)
    pts(:,a(i))=[];
end
points = pts;
end
